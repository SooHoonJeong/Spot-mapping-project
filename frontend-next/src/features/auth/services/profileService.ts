// TODO: pre-existing stub — `profileService` is an empty object and the `API` import above
// is unused. Left unimplemented per migration parity requirements (out of scope here).
import API from "../../../api/axios";

export const profileService = {};
